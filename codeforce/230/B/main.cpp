#include <bits/stdc++.h>

#define FOR(i,a,b) for(long long i = (a); i < (b); i++)
#define FORD(i,a,b) for(int i = (a); i >= (b); i--)
#define REP(i,a) for(int i = 0; i < (a); i++)
#define REPD(i,a) for(int i = (a)-1; i >= 0; i--)
#define sz(a) (int)a.size()
#define ALL(a) a.begin(),a.end()

#define ii pair<int,int>
#define fi first
#define se second
#define ll long long

using namespace std;

int main()
{
  /*  long long n;
    cin>>n;
    FOR(j,0,n)
    {
        long long x;
        cin>>x;
        long long sqr=sqrt(x);
        long long i=2;
        for(i;i*i<sqr;i++)if(sqr%i==0)break;
        if(i*i>sqr && sqr*sqr==x && x>1)cout<<"YES\n";
        else cout<<"NO\n";
    }*/
   /*  int n;
    int k;
    int i=0;
    cin>>n>>k;
    for(;i<=n;i++)
    {
        if(5*i*(i+1)/2+k>240)break;
    }
    cout<<i-1;*/
    /*int x;
    cin>>x;
    int sol1 = x/10;
    int sol2 = x%10 + x/100*10;
    cout<<max(sol1,max(sol2,x));*/


}
